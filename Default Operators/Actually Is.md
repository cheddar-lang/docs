# Actually Is Operator (`actually is`)

## Syntax
```
a actually is b
```

## Explanation
Determines whether `a` or `b` refer to the same **object**.

## Examples
```
1 actually is 1 // false

let a := 1
let b := a

a actually is b // true
```
