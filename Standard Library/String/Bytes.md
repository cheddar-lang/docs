# Bytes (`.bytes`)

| type | returns |
| ---- | ------- |
| Getter | `array<int>` |

## Explanation
Returns the bytes in the given string as an array of character codes.

## Examples

```swift
"foo".bytes == [102, 111, 111]
"".bytes == []
```
